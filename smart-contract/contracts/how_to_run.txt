- Terminal 1: Start the local blockchain 
npx hardhat node

- Terminal 2: deploy the smart-contract
// this is deployment
npx hardhat run smart-contract/scripts/deploy.js --network localhost
// if you want to test with deposit or withdraw, use the test folder
npx hardhat run smart-contract/test/test-bank.js --network localhost
npx hardhat test smart-contract/test/test-bank.js --network localhost

NOTE: if we change the solidity code then run this (Only do this if you changed your code)
$env:NODE_OPTIONS="--no-warnings"; npx hardhat compile


NOTE: npx hardhat compile (use this if you have never compile before)



CHECK Aave to see how much it pays to our smart contract
npx hardhat run smart-contract/scripts/check_profit.js --network localhost



reload the page
localStorage.clear(); 
location.reload();